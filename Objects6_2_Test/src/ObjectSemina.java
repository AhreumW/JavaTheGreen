
public class ObjectSemina {
/*
	Object 클래스?
			모든 클래스의 조상. (객체)
		Object클래스는 모든 클래스 상속계층도의 제일 위에 위치하는 조상 클래스이다. 
		다른 클래스로부터 상속 받지 않는 모든 클래스들은 자동적으로 Object 클래스로부터 
		상속받게 함으로써 이것을 가능하게 한다. 
		
	public class 클래스 extends Object{	//-> Object 클래스 묵시적 상속
		//우리는 계속 Object 클래스로부터 상속받아 확장시키고 있었다. 
	}	
	
	//
	상속받으면 나 자신의 것이 되므로 return값을 굳이 안 씀. void 메서드 사용.
	(return 값은 값을 주고받을때, 경로에서 사용될 때 사용)
	
	매개변수는 강력한 사용자의 의지를 전달할 때 사용.
*/
}
